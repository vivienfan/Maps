'use strict';

const express = require('express');
const router  = express.Router();

module.exports = (dataHelper) => {
  router.get('/:m_id', (req, res) => {

  });

  router.post('/:m_id', (req, res) => {

  });

  router.delete('/:m_id', (req, res) => {

  });

  return router;
}
